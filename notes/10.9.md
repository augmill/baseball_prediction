
# Notes

## Finalized roles

jackson - database and data read in\
austin - data exploration, feature engineering (can help with modeling)\
max - modeling\
august - minor data exploration,minor data processing, modeling

## Discussed

The larger model framework: create game state embeddings that are used to predict the at-bat outcome, another step could be using the des column we could make outcome sentence embeddings and learn weights to convert them to the sentence embedding and either with or without the game state one predict: what granularity of prediction do we want hit vs no hit; out vs on base vs homerun; etc. prob between 2 and 8 classes.

How to use and store vectors: need to look at if and how gcloud stores vectors so know about cost and capability

Maybe use curc for computing: gcloud can do some computation but we don't necessarily want to spend credits on that instead of querrying so CURC might be a good alternative if we need cloud computing

Concerns with weather data: obviously would be interesting and nice but given pybaseball don't have times for each pitch, we can't necessarily relate it to the weather data unless the weather data is generalized which might not be as useful

Issue around players and team representations: the player ids used in the pybaseball data are not obviously meaningful so we need to determine if that is true and if they are meaningful decide if they are meaningful in a useful way. If not we will need to create some kind of meaning for each player which might look like abstract embeddings, their stats, or a number increasing on when they joined the mlb. The same issue is true for the teams but we can just do an id from oldest to newest team.

What data to process and how: Max will have the doc with some info that should help determine what, August has some info on how.

For our recurrent meeting: We should decide if we need to meet at least a day or two before the day/ time we decide on in the when2meet

## To do

1. Fill out when2meet
2. Max send out doc and the rest look over
3. Look into how the player ids work
4. August, Jackson, and Max discuss how data can be converted for the models
5. Jackson build database
6. Austin explore data
7. August and Max begin building of models
8. Decide whether or not we want to include and use weather data
